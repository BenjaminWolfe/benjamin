if (file.exists("~/.Rprofile")) {
  base::sys.source("~/.Rprofile", envir = environment())
}

options(
  blogdown.new_bundle = TRUE,
  blogdown.title_case = TRUE,
  blogdown.ext = ".Rmd",
  blogdown.hugo.dir = "~/.hugo",
  blogdown.hugo.version = "0.63.1"
)
